
parameters:
    toolbar.bundle.controller.class: BackBuilder\Bundle\ToolbarBundle\Controller\ConfigController
    toolbar.bundle.plugin.manager.class: BackBuilder\Bundle\ToolbarBundle\Plugin\PluginManager

services:
    plugin.manager:
        class: %toolbar.bundle.plugin.manager.class%
        arguments: [@service_container]

    toolbar.controller:
        class: %toolbar.bundle.controller.class%
        arguments: [@bundle.toolbarbundle.config, @plugin.manager]
